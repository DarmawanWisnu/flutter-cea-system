@startuml
title Activity Diagram - Add Kit

|User|
start
:Akses halaman Add Kit;

|Sistem|
:Menampilkan form input Kit;

|User|
:Input ID kit;
:Input nama kit;
:Klik tombol Simpan;

|Sistem|
:Validasi data input;

if ("Data valid?") then (Ya)
  :Kirim request ke Backend API;
  :POST /kits {id, name};
  
  if ("Request berhasil?") then (Ya)
    :Menampilkan SnackBar "Kit berhasil ditambahkan";
    :Navigate ke Home Screen;
    stop
  else (Tidak)
    :Menampilkan pesan error;
    :(ID Kit sudah terdaftar / network error);
    :Kembali ke form Add Kit;
  endif
else (Tidak)
  :Menampilkan pesan kesalahan validasi;
  :(ID atau nama kosong);
  :Kembali ke form Add Kit;
endif
@enduml
