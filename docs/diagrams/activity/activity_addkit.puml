@startuml
title Activity Diagram - Add Kit

|User|
start
:Akses halaman Add Kit;

|Sistem|
:Menampilkan form input Kit;

|User|
:Input nama kit, ID kit, dan konfigurasi sensor;
:Klik tombol Simpan;

|Sistem|
:Validasi data input;
if ("Data valid?") then (Ya)
  :Menyimpan data Kit ke database lokal (SQLite);
  :Sinkronisasi Kit baru ke sistem MQTT;
  :Menampilkan notifikasi "Kit berhasil ditambahkan";
  stop
else (Tidak)
  :Menampilkan pesan kesalahan (contoh: ID Kit sudah terdaftar);
  :Kembali ke form Add Kit;
endif
@enduml
